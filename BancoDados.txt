SET SQL DIALECT 1;
CREATE DATABASE "c:\ProjDelphi6\locadora.gdb" PAGE_SIZE 4096
  USER "SYSDBA"
  PASSWORD "masterkey";

CONNECT "c:\ProjDelphi6\locadora.gdb"
  USER "SYSDBA"
  PASSWORD "masterkey";

			/* Tabela: Estilos */ 

CREATE TABLE ESTILOS ( 
  COD_ESTILO		INTEGER  	NOT NULL,   
  DESCRICAO		VARCHAR(50),
  
  CONSTRAINT PK_ESTILO PRIMARY KEY (COD_ESTILO)

);

		/* Tabela: Fitas */

CREATE TABLE FITAS (
  COD_FITA		INTEGER NOT NULL,
  DESCRICAO		VARCHAR(40) NOT NULL,
  OBSERVACAO		VARCHAR(100),
  DATA_CAD        	TIMESTAMP,
  TEMPO_DURA		VARCHAR(5),
  COD_ESTILO		INTEGER,
 
  CONSTRAINT PK_FITAS PRIMARY KEY (COD_FITA),
  FOREIGN KEY (COD_ESTILO) REFERENCES ESTILOS (COD_ESTILO)

);
